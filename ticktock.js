// Generated by CoffeeScript 1.4.0
(function() {
  var format_number, get_time;

  format_number = function(number) {
    if (number < 10) {
      return "0" + number;
    } else {
      return number;
    }
  };

  get_time = function() {
    var date, hrs, mins, secs;
    date = new Date;
    hrs = format_number(date.getHours());
    mins = format_number(date.getMinutes());
    secs = format_number(date.getSeconds());
    return "" + hrs + ":" + mins + ":" + secs;
  };

  Element.prototype.TickTock = function() {
    var me;
    me = this;
    me.innerHTML = get_time();
    return setInterval((function() {
      return me.innerHTML = get_time();
    }), 1000);
  };

}).call(this);
